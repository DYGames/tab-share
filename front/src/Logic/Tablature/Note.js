export default class Note {
    constructor (index, tempo, string, fret, note) {
        this.index = index;
        this.tempo = tempo;
        this.string = string;
        this.fret = fret;
        this.note = note;
    }
}